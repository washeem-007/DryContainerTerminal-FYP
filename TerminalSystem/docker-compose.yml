services:
  backend:
    build: 
      context: ./Server
    ports:
      - "5000:8080"
    volumes:
      - terminal_data:/app/data

  frontend:
    build: 
      context: ./Client
    ports:
      - "5173:80"
    depends_on:
      - backend

volumes:
  terminal_data:
